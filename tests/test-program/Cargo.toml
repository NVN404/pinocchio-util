[package]
name = "test-program"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]
path = "../test-program.rs"

[dependencies]
pinocchio = "0.9"
pinocchio-pubkey = "0.3"
pinocchio-utils = { path = "../.." }

[profile.release]
overflow-checks = true
lto = "fat"
codegen-units = 1
